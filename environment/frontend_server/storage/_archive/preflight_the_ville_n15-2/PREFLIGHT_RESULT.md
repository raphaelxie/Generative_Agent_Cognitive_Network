# Pre-flight verification result

| Field | Value |
|-------|--------|
| Status | **PENDING** — burn-in + `preflight_n15_batch.py` + `analyze_survey.py` not executed in this workspace |
| Last verify run | _(run `python verify_preflight_n15.py …/survey` after steps in PREFLIGHT.md)_ |

When `verify_preflight_n15.py` exits 0, replace **PENDING** with **PASS — ready for full n=15 pilot** and note the date.

If it exits non-zero, paste the script output here under **NOT READY**.
